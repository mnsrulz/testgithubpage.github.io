name: Greetings

on:
  schedule: 
    - cron: "* * * * *"

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy Stage
      uses: fjogeleit/http-request-action@master
      with:
        url: ${{ secrets.curl1 }}
        method: 'POST'
